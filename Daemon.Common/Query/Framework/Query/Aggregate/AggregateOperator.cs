namespace Daemon.Common.Query.Framework.Query.Aggregate
{
    public enum AggregateOperator
    {
        Count,
        DistinctCount,
        Distinct,
        Distinct100,
        Average,
        Max,
        Min,
        Sum,
        Range,
    }
}
